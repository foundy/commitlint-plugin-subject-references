const subjectTrailingReferencesEmpty = require('./rules/subjectTrailingReferencesEmpty');

module.exports = {
  rules: {
    'subject-trailing-references-empty': subjectTrailingReferencesEmpty
  }
};
